# 恢复说明：rebuild-v1-reset-20260802-ad4cf7f

1. 在独立 Git worktree 或新的临时目录操作，绝不覆盖当前活动层。
2. 从 `source/<原路径>` 复制需要检查的文件到新路径。
3. 使用 `manifest.json` 中的 SHA-256 与字节数校验副本。
4. 历史文本仅可作证据；要把任何内容重新变成活动设定或正文，必须获得作者逐条明确确认并记录在 `rebuild/reset-v2/author-confirmations.md`。
